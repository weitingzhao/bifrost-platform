version: "1"
lanes:
    - id: console-api
      track: build
      component_line: rocket
      track_type: build
      label: Console & platform-api
      short_label: Console
      description: Ops Console pages, Briefing/Control Room, auth, audit, actuation routes.
      agent_mode: Ops
      work_intent: feature
    - id: cluster-infra
      track: build
      component_line: rocket
      track_type: build
      label: Cluster & K3s tooling
      short_label: Cluster
      description: Node lifecycle, workload actuation, metrics-server, observability stack UI.
      agent_mode: Ops
      work_intent: cluster
    - id: gpu-pool-join
      track: build
      component_line: rocket
      track_type: build
      label: GPU pool join (placement residual)
      short_label: GPU pool
      description: |
        PARKED residual after workload-placement-governance CLOSED. Live placement evaluator: GPU pool status=planned (P5a gpu-server not joined); ai rule unsatisfied. Fleet is amd64-only (arm64 edge pool removed). Core amd64/nfs rules already OK. Do not reopen placement milestone — join GPU when ai-compute-strategy buy-signal fires.
      agent_mode: Ops
      work_intent: cluster
    - id: mcp-gitops
      track: build
      component_line: rocket
      track_type: build
      label: GitOps & MCP (P3–P5)
      short_label: GitOps
      description: Argo/Tekton execution, MCP server implementation (per MCP Contract), Agent SDK integration, Vision milestones V1–V5.
      agent_mode: Ops
      work_intent: feature
    - id: cicd-delivery
      track: build
      component_line: rocket
      track_type: build
      label: CI/CD bootstrap (P6)
      short_label: CI/CD
      description: L0/L1/L2 bootstrap model — CI gates (Tekton Trigger), deliver-prod pipeline, platform prod overlay, self-health probe, gate→spine closure, escape hatch runbook.
      agent_mode: Ops
      work_intent: feature
    - id: compose-k3s
      track: migrate
      component_line: satellite
      track_type: migrate
      label: Compose → K3s lift-and-shift
      short_label: K3s
      description: STG v2 + prod overlay on K3s (CLOSED). Native refactor → trade-k8s-native lane.
      agent_mode: Ops
      work_intent: ops
    - id: trade-k8s-native
      track: migrate
      component_line: satellite
      track_type: migrate
      label: Trade K8s-native + IB Edge
      short_label: K8s native
      description: 'Ideal K8s runtime: Traefik Ingress, Ops kubernetes executor, IB Lease active-standby, 3 gateways/env, dev mock. Authority: migrate-waves YAML.'
      agent_mode: Ops
      work_intent: cluster
    - id: data-layer-k3s
      track: migrate
      component_line: satellite
      track_type: migrate
      label: Data layer (PG + Redis)
      short_label: Data
      description: 'Lift stateful services to data NS: CloudNativePG (R-DV1 DBs), redis-live/queue per env, NAS backups. Spine stream data-layer-k3s.'
      agent_mode: Ops
      work_intent: cluster
    - id: legacy-retire
      track: migrate
      component_line: satellite
      track_type: migrate
      label: Legacy retirement (Phase 3)
      short_label: Retire
      description: 'SIGNED 2026-06-29 (decision D8): UI side-by-side gate dropped (Legacy already stopped; Phase 2B 9/9 domains business-equivalent). Legacy runtime stopped, bifrost-trader-engine NAS-archived read-only. UI polish continues as ordinary Design System work.'
      agent_mode: Ops
      work_intent: ops
    - id: trade-stack
      track: migrate
      component_line: satellite
      track_type: maintain
      label: Trade stack verification
      short_label: Verify
      description: Backend repos + frontend pages — reference closed streams for regression.
      agent_mode: Product
      work_intent: frontend
    - id: platform-gitops
      track: automate
      component_line: engineer
      track_type: build
      label: Platform GitOps & containerization
      short_label: GitOps
      description: Gitea mirror for bifrost-platform, Dockerfile (api + runner), K8s overlay, Argo CD Application, Tekton deliver-platform pipeline. Enables Mac Pro Cursor dev → Gitea → K3s auto-deploy.
      agent_mode: Ops
      work_intent: automate
    - id: agent-infra
      track: automate
      component_line: engineer
      track_type: build
      label: Agent host & Gateway
      short_label: Infra
      description: Hermes Gateway on Mac Mini, CTRL NODE Bridge, Staleguard/ctxharness tooling, iMessage/Telegram channel.
      agent_mode: Ops
      work_intent: automate
    - id: drift-remediation
      track: automate
      component_line: engineer
      track_type: maintain
      label: Drift detection & remediation
      short_label: Drift
      description: |
        Nightly catalog drift scan closed. Active Delivery Board program retrospective-agent (Waves B–C): DefectReport code attribution + fix-PR proposal. Spine stream retrospective-agent. Hermes L0 first-task closed 2026-08-08 (hermes-gateway-integration 6/6).
      agent_mode: Ops
      work_intent: automate
    - id: agent-services
      track: automate
      component_line: engineer
      track_type: build
      label: Agent services (MCP + Trade)
      short_label: Services
      description: |
        DONE — All agent-services streams closed: agent-mcp-integration 3/3, release-agent-task 3/3,
        mission-auto-remediation 5/5 (③ Trade-on-K3s residual deferred to trade-k8s-native),
        trade-k8s-migration 6/6, agent-release-discipline 5/5. Dev-agent--engineer-build program closed-superseded.
        Vision parked on Ready lanes agent-trade-advisory-parked + flight-director-parked.
      agent_mode: Ops
      work_intent: automate
    - id: network-server
      track: infra
      component_line: ground
      track_type: build
      label: Server LAN upgrade (router + switch)
      short_label: Server LAN
      description: 'UCG Max + USW-Pro-Max-24 deployment: VLAN 10/20/50, 2.5G uplinks, K3s node validation, kube-vip VIP.'
      agent_mode: Ops
      work_intent: ops
    - id: network-wifi
      track: infra
      component_line: ground
      track_type: build
      label: WiFi upgrade (U7 Pro + U6 Mesh)
      short_label: WiFi
      description: 'Replace Eero with UniFi APs: per-floor rollout, MoCA backhaul, VLAN SSIDs (Bifrost/Family/vision), coverage benchmark.'
      agent_mode: Ops
      work_intent: ops
    - id: governance
      track: operate
      component_line: operations
      track_type: maintain
      label: Governance & spine
      short_label: Gov
      description: Matrix probes, spine milestones, infra YAML, actuation guardrails, Vision/Blueprint/MCP Contract alignment.
      agent_mode: Ops
      work_intent: ops
    - id: troubleshoot
      track: operate
      component_line: operations
      track_type: maintain
      label: Troubleshooting
      short_label: Debug
      description: Failing probes, cluster reachability, workload errors, connectivity.
      agent_mode: Ops
      work_intent: debug
    - id: release
      track: operate
      component_line: operations
      track_type: release
      label: Release & promote
      short_label: Release
      description: Flywheel readiness, cutover blockers, release gate, prod matrix sign-off.
      agent_mode: Promote
      work_intent: release
    - id: platform-health
      track: operate
      component_line: rocket
      track_type: maintain
      label: Platform health & observability
      short_label: Health
      description: |
        DONE — Mission Signal program completed (7/7 gates signed, no_handoff). Ongoing uptime/latency/readiness/alert triage lives in Daily Ops / Control Room, not this delivery lane. Original intent: Console uptime, API latency, cluster workload readiness, self-health probes, alert triage.
      agent_mode: Ops
      work_intent: ops
    - id: network-monitoring
      track: infra
      component_line: ground
      track_type: maintain
      label: Network monitoring & health
      short_label: Monitor
      description: |
        DONE — network-monitoring-ops program completed (P0–P4 signed, no_handoff). Ongoing UniFi health/bandwidth/anomaly/SLA via Network page / Daily Ops; reuses platform-api + mcp/unifi (Network Governance stack).
      agent_mode: Ops
      work_intent: ops
    - id: polygon-vendor
      track: automate
      component_line: subcontractor
      track_type: build
      label: Polygon.io data pipeline
      short_label: Polygon
      description: Options/stock WebSocket feeds, historical OHLCV, snapshot APIs, rate-limit management, data quality.
      agent_mode: Ops
      work_intent: feature
    - id: market-data-expand
      track: automate
      component_line: subcontractor
      track_type: build
      label: Market Data Plugin expansion (Layer 1+2)
      short_label: MD Expand
      description: |
        DONE — 8/8 phases signed · Layer 1 (raw ingest) + Layer 2 (analytics) + Layer 3 (API/UI migration + cleanup). Vitals UI follow-on → market-data-vitals.
      agent_mode: Ops
      work_intent: feature
    - id: market-data-vitals
      track: automate
      component_line: subcontractor
      track_type: build
      label: Market Data Vitals UI
      short_label: MD Vitals
      description: |
        Data quality visibility upgrade for Ops Console Market Data page. Wave 1 (P1–P3): Overview Vitals strip + Today's Data verdict. Wave 2 (P4–P6): Stock depth + Option coverage + Coverage tab refactor. Wave 3 (P7–P8): Gap detection + Quality Score.
      agent_mode: Ops
      work_intent: feature
    - id: market-data-gs-closeout
      track: automate
      component_line: subcontractor
      track_type: build
      label: Market Data Golden Source Closeout
      short_label: MD Closeout
      description: |
        Residual closeout after golden-source + write-consolidation: Board close,
        leftover Trade tables, catalog copy, Ingest pending rollup, Plugin write auth,
        stock_daily depth A then B (watchlist+benchmarks then universe). Not C (13411).
        D10 BLOCKED. Do not unsuspend readiness-refresh. Do not scale Celery Massive.
        Successor program: market-data-gs-ops-closeout (ops residuals; do not reopen settled YAML).
      agent_mode: Ops
      work_intent: feature
    - id: ib-vendor
      track: automate
      component_line: subcontractor
      track_type: build
      label: Interactive Brokers lifecycle
      short_label: IB
      description: |
        DONE — IB Gateway Plugin delivery complete (ib-gateway-plugin 7/7 signed; launch-plugin-lane closed-superseded).
        Live evidence: ib-gateway 1/1 in data NS, host+secondary connected, redis-ib OK; D3 SIGNED; D10 BLOCKED (quotes-only).
        Publish/observe: Mission Launch · Launch Plugin + Plugin Gallery. Ongoing version / client ID / Lease = maintain patches, not this Init Build lane.
      agent_mode: Ops
      work_intent: feature
    - id: qa-describe-first-4894
      track: migrate
      component_line: satellite
      track_type: migrate
      label: QA Trade bars pipeline
      short_label: QA Trade bars
      description: |
        DONE — Acceptance covered by closed migrate spine (27/27) + trade-celery-k8s-ideal + market-data-subcontractor. Evidence: celery-worker-stocks-ib 1/1 on DEV/STG/PROD; market.stock_daily populated; Polygon daily via plugin-market-data; IB bars path retained on stocks_ib Celery. Original intent: Migrate legacy trade daemon bars backfill to K8s-native worker — acceptance test lane.
      agent_mode: Ops
      work_intent: feature
    - id: trade-system-celery
      track: build
      component_line: satellite
      track_type: build
      label: 分析目前Trade System使用的Celery 相关的后台作业调度方案
      short_label: 分析目前Trade System使用的Celer
      description: |
        DONE — Trade Celery / Massive K8s Ideal (W0–W5) signed complete. Verdict: keep Celery for IB bars (stocks_ib); Massive Polygon ingest moved off Celery to plugin-market-data (market-data-subcontractor). K8s Ideal for remaining Celery = per-queue Deployments + Beat + executor_mode=kubernetes (program trade-celery-k8s-ideal). Original question: 基于目前Trade System使用的Python语言，之前Trade采用的后台Task方案是Celery， 在基于k8s的运行环境下，和Trade 系统的业务需求，最Ideal的Worker 方式还是Celery吗？ 如果是的话，那目前Celery在目前环境下的运行方式也是Ideal的方式吗？
      agent_mode: Ops
      work_intent: feature
    - id: trade-iv-radar
      track: build
      component_line: satellite
      track_type: build
      label: Trade IV Radar
      short_label: IV Radar
      description: Wave A — Research IA (Ideas / Volatility / Structure / Risk / Lab) + IV Rank regime page (Benchmarks ∪ optionable Watchlist ∪ Holdings). D10 observe-only; A2 Event Radar out of scope.
      agent_mode: Product
      work_intent: feature
